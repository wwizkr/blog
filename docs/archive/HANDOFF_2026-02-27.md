# BlogWriter 작업 인수인계 문서 (2026-02-27)

## 1. 오늘까지 완료한 내용

## 1.1 프로젝트 스캐폴딩
- Python 데스크톱 앱 구조 생성 (`src/blogwriter`)
- 실행 엔트리 구성: `run.py`
- 의존성/설정 파일 구성:
  - `requirements.txt`
  - `pyproject.toml`
  - `.gitignore`

## 1.2 UI 기본 프레임
- 메인 창 90% 크기 정책 적용
- 사이드바 메뉴 구성:
  - 대시보드
  - 수집 실행
  - 라벨링
  - 글 생성/에디터
  - 발행 관리
  - 설정/키워드

## 1.3 DB/모델/마이그레이션
- SQLite + SQLAlchemy + Alembic 구성
- 마이그레이션 파일:
  - `0001_initial.py`
  - `0002_collection_tables.py`
  - `0003_label_tables.py`
  - `0004_generated_articles.py`
  - `0005_publish_jobs.py`
  - `0006_channel_settings_and_image_source.py`
  - `0007_publish_channels_and_api_url.py`

## 1.4 수집 파이프라인
- 크롤러 베이스 + 채널 수집기 구성
- `CollectorManager`가 크롤러 모듈 자동 탐색/등록
- 수집 시 저장:
  - 원문: `raw_contents` (`source_url` 유니크)
  - 이미지: `raw_images` (`source_url` 유니크, `page_url` 기록)
- 중복 수집 차단:
  - 글/이미지 모두 출처 URL 기준 중복 방지

## 1.5 라벨링
- 규칙 기반 텍스트/이미지 라벨링 서비스 구현
- 라벨링 화면에서 미라벨링 데이터 일괄 라벨링 가능

## 1.6 글 생성/에디터
- 형식별 초안 생성:
  - `blog`
  - `sns`
  - `board`
- 생성 글 목록 조회/선택/수정/저장 구현

## 1.7 발행
- 발행 큐 등록/처리 화면 구현
- 발행 작업 테이블/상태 추적 구현
- `publish_channel_settings.api_url` 저장/표시/사용 연동(로그 메시지 반영)

## 1.8 채널 분리(중요)
- 수집채널: `source_channels`
- 발행채널: `publish_channels`
- 발행 설정: `publish_channel_settings`
  - 발행 주기
  - 모드
  - 발행 형식
  - 작성 형식
  - API URL

---

## 2. 실행 확인 결과
- 의존성 설치 완료 (`PySide6`, `alembic` 등)
- UI 실행 확인:
  - 명령 실행 시 GUI 프로세스가 떠서 타임아웃으로 반환됨(정상 실행 패턴)
- 실행 중 순환참조 에러 1건 수정 완료:
  - `collector/__init__.py`
  - `ui/collection_page.py`

---

## 3. 내일 바로 시작할 작업 (우선순위)

## P1. 페르소나 관리(신규 추가)
1. `personas` 테이블 추가 (이름, 톤, 스타일 가이드, 금칙어, 활성여부)
2. 관리자 화면에서 페르소나 CRUD 구현 (여러 개 생성/수정/삭제/활성화)
3. 글 생성 화면에서 페르소나 선택 드롭다운 연동
4. 생성 결과에 사용 페르소나 ID/이름 저장

## P1. AI API 관리 목록(신규 추가)
1. `ai_providers` 테이블 추가:
   - provider, model_name, api_key_alias
   - is_paid(유료/무료), is_enabled(사용/미사용)
   - priority, rate_limit_per_min, daily_budget_limit
   - status(정상/오류/차단), last_checked_at
2. 관리자 화면에서 API 목록 CRUD:
   - 유료/무료 구분 표시
   - 사용/미사용 토글
   - 우선순위 편집
   - 연결 테스트 버튼
3. 글 생성기 연동:
   - 활성 API만 사용
   - 우선순위 기반 호출 + 실패 시 fallback
   - 무료 우선/유료 우선 모드 옵션
4. 운영 로그:
   - API별 호출 수/성공률/오류 내역 기록

## P1. 발행 채널 실제 연동
1. `publisher/service.py`에서 실제 HTTP 발행 구현
2. 채널별 인증 헤더/토큰 저장 구조 추가
3. 성공/실패 응답 파싱 및 `publish_jobs.message` 정리

## P1. 수집기 안정화
1. 네이버/티스토리 본문 파싱 정확도 개선
2. iframe/리다이렉트/상대경로 URL 보정
3. 실패 원인별 재시도 정책 추가

## P2. 설정 화면 정리
1. 설정 화면 영역 분리(카테고리/키워드 vs 발행설정)
2. 발행채널 전용 테이블에 활성상태 표시 컬럼 추가
3. API URL 유효성 검사(형식 체크) 추가

## P2. 이미지 처리 보강
1. `raw_images` 다운로드 단계 추가(`local_path` 저장)
2. 파일 유효성/사이즈 기준 필터링

## P3. 테스트
1. Repository 단위 테스트
2. 수집->라벨링->작성->발행 큐 통합 테스트
3. 마이그레이션 업/다운 기본 검증

---

## 4. 내일 작업 순서(권장)
1. 마이그레이션 최신 적용 확인 (`alembic upgrade head`)
2. 페르소나 테이블/마이그레이션 + 관리자 화면 CRUD 먼저 구현
3. AI API 관리 테이블/마이그레이션 + 관리자 화면 CRUD 구현
4. 글 생성 화면 페르소나/API 정책 선택 연동 확인
5. 설정 화면에서 발행채널/API URL 입력 동작 확인
6. `PublisherService` 실연동 구현
7. 수집기 파싱 개선
8. 회귀 테스트 및 문서 업데이트

---

## 5. 빠른 실행/점검 명령

작업 디렉토리:
- `D:\Project\TripReview\blogWriter`

의존성 설치:
```powershell
C:\Users\wwizc\AppData\Local\Programs\Python\Python312\python.exe -m pip install -r requirements.txt
```

마이그레이션:
```powershell
set PYTHONPATH=src
alembic upgrade head
```

앱 실행:
```powershell
C:\Users\wwizc\AppData\Local\Programs\Python\Python312\python.exe run.py
```

---

## 6. 현재 주의사항 / 알려진 이슈
- `PublisherService`는 아직 샘플 처리이며 실제 외부 API POST는 미구현.
- 수집기는 기본 파싱만 적용되어 사이트 DOM 변경에 취약할 수 있음.
- 런타임 검증은 수동 UI 확인 중심으로 진행됨(자동 UI 테스트 미구현).

---

## 7. 내일 시작 전 체크리스트
- [ ] `alembic upgrade head` 성공
- [ ] 페르소나 2개 이상 생성/수정/삭제 동작 확인
- [ ] 생성 시 선택 페르소나가 글 데이터에 저장되는지 확인
- [ ] AI API 목록 2개 이상 등록 후 유료/무료/사용여부/우선순위 설정 확인
- [ ] API 연결 테스트 및 fallback 동작 확인
- [ ] 설정 화면에서 발행채널/API URL 저장 확인
- [ ] 글 1건 생성 후 발행 큐 등록 확인
- [ ] `PublisherService` 실연동 브랜치 생성 후 개발 시작
