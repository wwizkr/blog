# 03. 수집

## 서브메뉴 (설정 우선)
1. 수집 설정
2. 수집 실행
3. 작업 이력
4. 수집 데이터

## 목적
- 수집 파이프라인 운영 및 결과 확인

## 수집 설정 화면 (현재)
- 수집 주기(분), 키워드당 최대 수집, 타임아웃(초), 재시도(회)
- 수집 선택: 전체 수집 / 체크된 내역만 수집 / 키워드 확장(연관키워드 포함)
- 채널 목록 체크박스 + 전체 선택 + 선택 개수 표시
- 카테고리 체크박스 + 전체 선택 + 선택 개수 표시
- 저장 / 기본값 복원
